# Archive — crossfit-dash-pro standalone migrations

These 12 SQL files are the Lovable-autogenerated migration history of
**crossfit-dash-pro's own throwaway Supabase project** (`hqnpvizssxrtmusaqsjg`),
from before that app was ported onto the shared WODPLACE database.

**They were never applied to `wiwpaekdykxernegicdv` (the shared DB) and never
will be.** They build a different schema — verified mismatches against the live
shared DB:

| these migrations create | shared DB actually has |
|---|---|
| `profiles(id, full_name, avatar_url, created_at)` | `profiles(id, email, created_at)` |
| `box_settings(id, invite_code)` | `box_settings(box_id, key, value, updated_at)` |
| table `contracts` | no such table (`contract_documents` instead) |
| enums `app_role`, `member_status`, … | none of them exist |
| `has_role()`, `handle_new_user()` | not present |

Kept only so the pre-port history of the box-admin app isn't lost. Do not run
any of these. The migration that actually adapted the panel to the shared DB is
`supabase/migrations/20260830190000_wodplace_admin_panel_port.sql`.
